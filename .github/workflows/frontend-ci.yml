name: Frontend Continous Integration

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Audit Dependencies
        run: npm audit -w frontend
      
      - name: Install dependencies
        run: npm ci

      - name: Linting check
        run: npm run lint -w frontend

      - name: Build Shared Package
        run: npm run build -w shared

      - name: Run Tests
        run: npm run test -w frontend

      - name: Production Build
        run: npm run build -w frontend

        env:
          ENV: production
          CLOUDFLARE_DUMMY_KEY_INVISIBLE_PASS: 1x00000000000000000000BB
          CLOUDFLARE_DUMMY_KEY_INVISIBLE_BLOCK: 2x00000000000000000000BB
          NEXT_PUBLIC_CLOUDFLARE_SITE_KEY: 0x4AAAAAACAJ7tYYt340DZW7
          NEXT_PUBLIC_BACKEND_BASE_URL: http://localhost:3001
          NEXT_PUBLIC_BACKEND_DOCKER_INTERNAL_URL: http://backend:3001
          NEXT_PUBLIC_DOCKER_ENV: "false"
          NEXT_PUBLIC_STRIPE_ENV: testing
          NEXT_PUBLIC_STRIPE_PUBLIC_KEY: pk_test_51SQQEHDSklUwZqQ3k0jdNLJZCuWOKxCHPS599G9vjIYEIW1NHQmfeW9eUazGIYnEb6r77oojkZTTYXH40F1lnZTU00HHdoRBOR
          NEXT_PUBLIC_DOMAIN_NAME: Shoup
          NEXT_PUBLIC_DOMAIN: http://localhost:3000/