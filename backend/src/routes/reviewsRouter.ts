import { Router } from "express";

const reviewsRouter = Router()

//make a get all reviews that you can filter, sort and paginate for the admin, doesnt have to be protected

export default reviewsRouter